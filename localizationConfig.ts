import LocalizedStrings from 'react-localization';
import * as arTranslations from './src/locales/ar.json';

const localization = new LocalizedStrings({
  ar: arTranslations,

});

export default localization;